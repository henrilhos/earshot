# @earshot/worker

Placeholder. The Instance lands here: the JSON API, the SPA served through
Static Assets, and the scheduled tick, all on one origin with state in D1.

It runs `packages/core` on `workerd`, which is why the core carries no `node:`
imports (ADR-0003).

See `.scratch/saas/issues/09-instance-api.md` and
`.scratch/saas/issues/06-scheduler.md`.
