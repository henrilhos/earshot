# @earshot/web

Placeholder. The SPA lands here: sign in, your Subscriptions, and the
Deliveries screen that answers "why didn't it queue that song?".

Vite + React, with its own dependency tree and its own build step. Neither may
reach `packages/cli`, which is why the boundary is a workspace and not a
directory.

See `.scratch/saas/issues/12-spa.md`.
